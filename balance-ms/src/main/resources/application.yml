server:
    port: ${PORT:8002}

spring:
    application:
        name: balance-ms
    jpa:
        database: mysql
        hibernate:
            ddl-auto: update
    datasource:
        url: jdbc:mysql://${MYSQL_HOST:localhost:3306}/balance-ms?createDatabaseIfNotExist=true
        username: root
        password: root
        driver-class-name: com.mysql.cj.jdbc.Driver